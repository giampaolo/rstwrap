..
    Regression: an unsplittable token (e.g. a long hyperlink) in a
    prose paragraph would make wrapping produce an over-width line.
    Keep the paragraph verbatim.

See the `prebuilt binary packages are
available <https://docs.python.org/dev/download.html>`_.  Also see other stuff that matters.
