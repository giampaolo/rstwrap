..
    Regression: a list item with an unsplittable token (long
    hyperlink) should still be re-wrapped. The token line may exceed
    the target width, but no wrap-produced line is longer than the
    longest source line.

- See the `prebuilt binary packages are
  available <https://docs.python.org/dev/download.html>`_.  Also see other stuff that matters here.
- A normal short bullet.
